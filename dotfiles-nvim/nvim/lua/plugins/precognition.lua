-- precognition.nvim: virtual text hints for Vim motions at the cursor.
-- Disabled again via lua/plugins/disabled.lua (user preference): lazy.nvim
-- skips disabled specs entirely, so opts/event below are dormant placeholders.
return {
  "tris203/precognition.nvim",
  event = "VeryLazy",
  opts = {},
}
